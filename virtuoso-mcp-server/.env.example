# Virtuoso MCP Server Configuration

# Path to the Virtuoso API CLI binary
VIRTUOSO_CLI_PATH=/path/to/virtuoso-GENerator/bin/api-cli

# Path to the Virtuoso configuration file (optional)
# If not set, will look in ~/.api-cli/virtuoso-config.yaml
VIRTUOSO_CONFIG_PATH=/path/to/virtuoso-config.yaml

# Enable debug logging (optional)
DEBUG=false

# Node environment (development/production)
NODE_ENV=production
